<article class="post labs container">
    <div class="well post-body first-post last-post">
        <h1><%- page.title %></h1>


        <div class="time-info">
            <!--发表于: <time datetime="<%= date_xml(page.date) %>"-->
                       <!--itemprop="datePublished"><%= page.date.format(config.date_format) %></time>-->
            <!--<% if (page.updated!=null) { %>-->
            <!--更新于: <time datetime="<%= date_xml(page.updated) %>"-->
                       <!--itemprop="dateModified"><%= page.updated.format(config.date_format) %></time>-->
            <!--<% } %>-->
        </div>

        <div class="post-body-inner">
            <%- page.content %>

            <%- partial('_partial/comment') %>
        </div>
    </div>
</article>
